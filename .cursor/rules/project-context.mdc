---
description: LogiTrainer Studio — contexto canónico del proyecto (memoria persistente)
alwaysApply: true
---

# LogiTrainer Studio

## Ubicación y repos

- **Código canónico:** `c:\proyectos\logitrainerstudio`
- **GitHub:** https://github.com/Lumpenvisual/logitrainerstudio
- **Memoria humana:** `PROJECT-MEMORY.md` | **Auditoría:** `AUDIT-REPORT.md`

## Despliegue

- **Producción:** https://logitrainerstudio.vercel.app
- **Deploy:** `npx vercel deploy --prod --yes`
- **SPA:** `vercel.json` rewrites a `index.html`

## Acceso unificado `/studio`

- Contraseña: `LTS-Mayo2026-7kQ!` — `src/lib/studioHub.ts`, localStorage `lts_studio_hub_session`
- Rutas: `/studio`, `/studio/login`, `/studio/dashboard`
- `SiteAccessGuard` redirige a `/studio/login` si no hay sesión
- Código: `StudioHub.tsx`, `useStudioAuth.ts`, `TunnelRootRedirect.tsx` (trycloudflare → `/studio`)

## Túnel localhost:8080

- Quick Tunnel: `npm run publish:trycloudflare` | URL: `TRYCLOUDFLARE-URL.txt`
- Persistencia: `LogiTrainerStudio-TunnelStack`, `LogiTrainerStudio-TunnelWatchdog`
- Docs: `docs/CLOUDFLARE-TUNNEL.md`

## Supabase (propietario)

- **Project ref:** `zghzhfheyawvbdddsybe`
- **Secrets:** `GEMINI_API_KEY` (no en repo)
- **Functions:** `npx supabase functions deploy <name> --project-ref zghzhfheyawvbdddsybe`

## IA

- Gemini nativo: `supabase/functions/_shared/gemini.ts`
- Cliente: `aiService.ts` + `edgeClient.ts`
- Smoke: `npm run test:ai-apis` | E2E: `npm run verify:prod` (10) | Túnel: `npm run tunnel:verify` (4)

## Auth back-office

- `backoffice@logitrainerstudio.app` / `LTS-BackOffice-2026!mX`
- Tras hub: `/auth` — `passSiteGate()` en `tests/e2e/helpers/studio.ts`

## Demo

- `/demo` público — `public/demo/logitrainer/` — `npm run demo:generate`

## Verificado (2026-05-19)

- `npm run audit:lts` + `verify:prod` 10/10 + `tunnel:verify` 4/4

## Skills

`lts-supabase-backend`, `lts-edge-client`, `lts-gemini-api`, `lts-e2e-verification`, `lts-demo`, `lts-studio-hub`
